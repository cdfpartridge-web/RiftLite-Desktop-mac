# RiftLite continuation handover — 2026-09-04

**Latest continuation:** [Maintenance implementation, build cleanup and Git checkpoint](./MAINTENANCE-2026-09-04.md) supersedes the local status below. Its latest candidate completed on 2026-09-04 at 22:35 BST. It records the authorized maintenance changes, passing 189-file / 1,861-test gate, earlier native Atlas checks, removal of 888 obsolete renderer assets and fresh 159 MB local installer hashes. Live gameplay validation was explicitly left pending. Public v0.9.72 remains unchanged.

The sections below preserve the earlier **20:34 BST source-only checkpoint**, including the Atlas defect evidence and original changes. [START_HERE.md](./START_HERE.md) is the entry point; [HANDOVER-2026-08-30.md](./HANDOVER-2026-08-30.md) retains the detailed project, architecture, feature and release history.

## Start in the correct repository

```text
C:\Users\cdfpa\OneDrive\Documents\Claude\Projects\Riftlite Beta 0.6\desktop-v06
```

The Codex project may open at `C:\Users\cdfpa\OneDrive\Documents\Claude\Projects\RiftLite`. That is the legacy project folder, not the active Electron source. Read the canonical repository's `AGENTS.md`, then run its read-only snapshot and inspect status:

```powershell
Set-Location -LiteralPath 'C:\Users\cdfpa\OneDrive\Documents\Claude\Projects\Riftlite Beta 0.6\desktop-v06'
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\print-handover-state.ps1
git status --short
```

- Branch: `hotfix/atlas-shell-recovery`; no configured upstream.
- HEAD: `e6b5a79001efe1b63cf93f39d95094ac65052f53` (`docs: record v0.9.72 publication evidence`).
- Package version: `0.9.72`.
- Public Windows/macOS release source: `19267728332ebb9c946d7a61d03715c149400589`.
- The current Atlas follow-up exists in uncommitted source only. No build, install, version bump, commit, push or publication contains it yet.
- This handover request authorized documentation and continuity work. The user previously asked to wait for a rebuild request. Continue from the next user instruction; do not treat an earlier completed release request as an instruction to publish this new fix.

## Immediate issue and completed repair

A tester on Windows 11 with a 3840 × 2160 display still could not use Atlas Find Match or Host Room after upgrading to v0.9.72. The Player name input was absent and the new warning merely described the problem. The tester's Windows display scaling and app window dimensions were not supplied; 200% was a test scenario, not a confirmed tester setting.

Evidence remains at:

- `C:\Users\cdfpa\Downloads\riftlite-capture-events (76).jsonl`
- `C:\Users\cdfpa\AppData\Local\Temp\codex-clipboard-f05198e8-a7b5-4a20-85e0-69bd29e494ff.png`

Both existed at this checkpoint. The screenshot is in a temporary folder and may later disappear. Do not commit or publish tester diagnostics or browser profiles.

Log 76's v0.9.72 segment starts at line 169, `2026-09-04T17:49:34.947Z`. Seven lobby loads produced seven failed repair attempts, 25 collapsed-field reports and no successful repair. There was no room/match transition. Pointer acknowledgements showed the document visible and focused, pointing away from the earlier focus-only theory.

The normal lifecycle installs `atlasCardRenderingCssForUrl()` before the collapse is checked. v0.9.72's controller then inserted the same CSS again. A native Electron reproduction confirmed this can leave the input at `0×0`. The old probe had tested late first-time insertion and therefore missed the real lifecycle.

The local fix introduces `atlasLobbyPlayerFieldRepairCssForUrl()`: a distinct fallback for the exact HTTPS Atlas origin and idle/localized lobby routes. It gives the known structural wrapper a formatting context, relocates the named container, restores the existing Player section/input geometry and keeps the room-action region in layout. Main now inserts this fallback after the existing two stable collapsed measurements.

Preserve the controller's current-document, guest-identity, capture/matchmaking, SPA-cancellation and one-attempt-per-document guards. It does not read or set player names, clear site data, sign in, reload or start play. Do not reintroduce fixed grid rows/columns: an early draft hid room actions at DPR 2. The final action-region minimum-size guard is intentional.

## Exact local changes to preserve

Seven tracked files are modified from HEAD:

| File | Purpose |
| --- | --- |
| `src/shared/atlasLobbyPlayerField.ts` | Dedicated fallback CSS and exact-origin/lobby URL helper |
| `src/main/main.ts` | Wire the existing repair controller to the dedicated helper |
| `tests/atlasLobbyPlayerField.test.ts` | Scope, distinct fallback and layout-property regressions |
| `tests/atlasLobbyPlayerFieldIntegration.test.ts` | Reject the old generic-CSS repair wiring |
| `scripts/probe-atlas-player-field-layout.cjs` | Reproduce preinstalled CSS, reload, focus and responsive geometry |
| `docs/START_HERE.md` | Current entry point and new-chat prompt |
| `docs/HANDOVER-2026-08-30.md` | Detailed project history and updated operational facts |

This new `docs/HANDOVER-2026-09-04.md` is an additional intended, initially untracked handover file. The unchanged controller lives in `src/main/services/atlasLobbyPlayerFieldRepair.ts` and its behavior tests in `tests/atlasLobbyPlayerFieldRepair.test.ts`.

Five separate historical untracked entries must remain untouched: `docs/RiftLite-v0.9.70-Whats-New.docx`, `docs/RiftLite-v0.9.70-Whats-New.txt`, `docs/release-notes-v0.9.70.md`, `tmp-app-diff.txt`, and `tmp/`. The latter can contain sensitive browser state. Never use broad staging or cleanup; inspect unexpected changes and stage explicit intended paths only when committing is requested.

## Verification and remaining limits

Completed against the final source in the previous implementation turn:

- TypeScript lint passed; Atlas/game-Webview checks passed **33 files / 357 tests**.
- Complete clean rerun passed **181 files / 1,802 tests**. The first full run hit the known unrelated five-second `releasePackaging.test.ts` timeout; isolation and the complete rerun passed.
- Native Electron probe passed with generic compatibility CSS installed at `dom-ready`, a healthy document, same-URL reload, fallback recovery, focus, protected guest, duplicate/SPA budgets and zoom.
- Independent public signed-out Atlas checks restored fields to approximately 283–391 × 44 at 1280 × 900, 1690 × 945 and 1904 × 993 CSS viewports. Find/Host/Solo/Join remained visible, enabled, focusable and centre hit-testable.
- A separate native DPR 2 run preserved a healthy narrow layout. The host is not physically 4K: the requested DPR 2 window was capped to roughly 952 × 498 CSS pixels; the separate 1904px viewport approximated a 4K panel at 200% scaling.
- Independent review found no actionable P1/P2 issue. Optional hardening remains: the checked-in probe records action bounds but does not assert each action's enabled/focus/hit-test status, and its healthy-document branch is conditional. The independent live matrix tested these properties.

The fallback increases the current wide side-panel height by about 64px. The tested controls still fit and accept hit tests, but signed-in, installed-app acceptance on the tester's actual display remains outstanding. No room was started, no matchmaking was exercised and no recorded match was validated by the signed-out probes. A layout pass is not proof of an end-to-end successful match.

For future changes, focused checks are:

```powershell
npx vitest run tests/atlasLobbyPlayerField.test.ts tests/atlasLobbyPlayerFieldRepair.test.ts tests/atlasLobbyPlayerFieldIntegration.test.ts tests/atlasCardRendering.test.ts
npm run lint
git diff --check
```

The native helper is `scripts/probe-atlas-player-field-layout.cjs`, run with the bundled Electron executable in a hidden process and `ELECTRON_RUN_AS_NODE` absent from the child environment. It compiles diagnostic source in memory without an app build, creates an isolated temporary profile and loads only the public signed-out lobby. Optional environment inputs are `RIFTLITE_ATLAS_PROBE_WIDTH`, `RIFTLITE_ATLAS_PROBE_HEIGHT`, `RIFTLITE_ATLAS_PROBE_SCALE`, and `RIFTLITE_ATLAS_PROBE_VERBOSE`. Inspect the reported actual viewport/DPR, not only requested dimensions. Output is under ignored `output/playwright/atlas-player-field-repair*`; those filenames can be overwritten on rerun. Repeating all passing checks is unnecessary for a documentation-only continuation.

## Next steps when requested

1. A local rebuild request should produce a fresh Windows candidate from the current source using the established `npm run electron:build` pipeline (`--publish never` is built in). Clear `ELECTRON_RUN_AS_NODE` for its child environment, record the exact installer/ASAR hashes and distinguish the candidate from public v0.9.72. Launching existing `dist/` or the old installer will not test this fallback.
2. On that candidate, verify the actual tester DPI/window configuration, Player field visibility/typing, first load/reload, Home-to-Play and alt-tab recovery. With a signed-in disposable test session, check real Find Match/Host Room transitions and repeat with replay video off/on. If a collapse occurs, confirm `atlas-lobby-player-field-repaired`; a healthy field may never need a repair event. Complete a controlled match and verify capture lifecycle and replay playback.
3. If publication is requested, select a new version above 0.9.72, update build identity/tests/customer notes, commit explicit intended source, build fresh Windows and native macOS artifacts from immutable source and verify downloaded assets/updater manifests. No next version has been selected. Preserve existing tags/assets and do not rerun the successful old tagged Mac job. A desktop Atlas fix requires no website/Firestore deployment.

## Release, website and broader project state

The canonical local `release\RiftLiteBetaInstall.exe` is still the public v0.9.72 installer: **159,339,221 bytes**, SHA-256 **`7E27AA6E2EAC45F25A9F94C8D2223B2B191AB173B3A6775602E537747D0ED5D4`**. It does not contain the new fallback. Public release and download verification was completed earlier on 2026-09-04; this handover refreshed local state only.

Release remotes are split: `windows` is `cdfpartridge-web/RiftLite-Desktop`; `origin` is `cdfpartridge-web/RiftLite-Desktop-mac`. Both v0.9.72 tags peel to `1926772`; local fetched main refs point to `e6b5a79` and may become stale. macOS uses native GitHub runners; existing apps are ad-hoc signed and not notarized. The full historical handover contains asset hashes, workflow IDs, known packaged-smoke IPC caveats and clean-checkout line-ending requirements.

The website worktree is `C:\Users\cdfpa\OneDrive\Documents\Claude\Projects\RiftLite\.codex-worktrees\zelonius-web-20260826`, branch `codex/public-cards-up-combiner`, clean at `135d2398f33f77888adb3716b87ba81d16966b8d`. It matches the last verified deployed website source. The older `RiftLite-website` checkout is at `f94ab54` with two generated-file changes; preserve it. No listeners were found on 3000, 3001, 3002, 5173, 5174 or the historical replay-fixture port 3107 at this checkpoint.

Earlier requested work is already implemented/released: editable Scorepad, deck image sharing, Deck Insights/Card Review and Copy report, Enhanced Insights/Mark decision, voiceover/export fixes and volume control, replay markers, and X0TCG spotlight. Web clips/shared notes, alternate art, score/game tags, Cards Up/combiner, sideboard recovery and X0TCG video rotation are live per the prior deployment record. Replay Coach remains Coming Soon; Search Rules remains hidden. Read the detailed project handover or `WEB_REPLAY_SYSTEM_HANDOVER.md` for the next relevant feature instead of reimplementing earlier chat requests. Real-game acceptance items recorded there remain open.
